[project]
name = "armi-kernel"
version = "0.0.0"
requires-python = ">=3.14,<3.15"
dependencies = [
    "pyyaml==6.0.3",
]

[build-system]
requires = ["uv_build>=0.11.33,<0.12"]
build-backend = "uv_build"
