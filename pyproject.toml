[project]
name = "armi-workspace"
version = "0.0.0"
requires-python = ">=3.14,<3.15"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "packages/armi-kernel",
    "apps/armi-runtime",
    "apps/armi-admin",
]
