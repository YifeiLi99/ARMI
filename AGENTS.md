# ARMI 项目协作规则

本文件补充全局 `AGENTS.md`，作用于整个仓库。只记录 ARMI 特有的事实、边界和工作入口；通用的安全、Git、Windows、实现与验证规则继续遵循全局规则。

## 1. 仓库定位与当前阶段

- ARMI 是承载单个自主电子人长期存在的运行时，不是通用聊天助手、多角色平台或多 Agent 编排框架。
- ARMI 的基本骨架和既定内部功能已经完成：M0-Core、P0 核心旅程以及 P1 内部功能与旅程均已验收。现在进入由用户逐项指导的修正阶段，不再按历史 M0/P0/P1 步骤清单施工，也不自动推导下一阶段。
- 修正阶段以用户指出的实际问题、体验和新判断为入口。先核对当前代码与 01—09 的正式设计，再做范围内的最小修正；不得借修正之名自行扩展外部程序、真实账号、网页自动化、其他执行器、多模态/语音/形象、公开发布、安全、部署、迁移或发布工作。
- 开始任务时先读根目录 `README.md`、`docs/README.md`、与问题直接相关的正式设计及当前代码。历史逐步清单和实施流水已经移除，不再作为事实来源或工作入口。
- `docs/` 是私有设计资料并被 Git 忽略，但仍是本仓库的设计事实来源；不得因其未被 Git 跟踪而跳过读取或同步修改。
- 项目尚未授予开源许可证。不得擅自声明开源、复制不兼容代码，或删除研究资料中的来源与许可证记录。

## 2. 事实来源与设计顺序

- 正式设计依赖顺序为：产品范围 → 需求 → 领域 → 总体架构 → 业务流程与契约 → 数据库 → 详细实现 → 测试验收 → 发布运行。
- 处理某一层时，先读该层总览、直接上游和任务涉及的专题文件；不要用下游实现便利反向改写上游事实。
- `docs/00-外部研究参考/` 只提供研究证据和候选设计，不是 ARMI 的正式设计事实真源。外部项目的做法只有被正式设计正文吸收后才对 ARMI 生效。
- 文档状态只有 `草案`、`已确认`、`已废弃`。草案可供继续设计，但不能伪装成已确认结论；依赖草案作判断时明确说明。
- 发生冲突时修正最接近事实源头的文档，并同步所有受影响的下游契约、追踪关系和索引；不以补丁说明、并列旧方案或兼容文字长期保留互斥结论。
- 只有仍影响当前设计的重要取舍才进入 `docs/10-设计决策/`；讨论过程、临时待办、评审清单和已被正文吸收的研究过程不进入最终设计。

## 3. 文档编写约定

- 正文使用简体中文、UTF-8 和 LF；项目内链接使用相对路径，术语、需求编号、流程编号、表名和状态值保持原样。
- 每份正式设计文档应独立可读，开头标明状态，并在适用时记录版本、日期、上游依据和覆盖的需求编号。
- 需求必须有稳定编号、优先级和可验证验收条件；领域、架构、流程、数据与测试结论必须能够向上追溯。
- 各层只回答本层问题：需求不预设实现，领域不预设表结构，架构不抢先定义字段，流程不决定 API 或消息中间件，数据库不以 ORM 便利改变事实所有权。
- 只保留当前有效结论。修改正文后清理失效段落、旧术语、旧入口和重复定义；状态或目录变化时同步 `docs/README.md`。
- 引用外部研究时区分项目自述、源码或数据证据、复现实验结果和 ARMI 的推断；不能把项目声明写成已验证事实。
- 不为显得完整而填入占位方案、模拟结果或未经确认的技术选型。未知项应明确边界和所需证据。

## 4. 不可破坏的产品与架构不变量

- 正常运行中只有一个 ARMI、一条权威生命线和一个当前被承认的活动运行世界；进程、模型、设备、入口、活动和外部 Agent 都不是另一个主体。
- 主体、生活、关系、权限和外部效果各有唯一事实所有者与正式写入路径。客户端、缓存、索引、摘要、向量和其他派生读取不能反向覆盖权威事实。
- 首版采用单一权威运行时的模块化单体。依赖方向为 `Interface → Application → Domain`，适配器通过稳定内核契约接入；领域不得依赖具体模型 SDK、平台 SDK、数据库对象或客户端格式。
- 模型、网页、平台、工具和外部执行器均处于信任边界之外：它们只能提供候选、证据或执行回执，不能直接提交主体状态、扩大权限或定义真实结果。
- 主观记忆与客观记录严格分离。日志不能在日常认知中补全已遗忘内容，记忆变化也不能改写正常运行记录。
- 系统权限回答“能不能”，ARMI 的主观意愿回答“想不想”。创造者的系统管理权不等于社会关系中的绝对命令权。
- Codex→ARMI 管理 MCP 与 ARMI→Codex 受限执行适配器必须隔离，不能相互发现、调用、继承凭据或串联权限。
- 对外表达和现实动作必须先登记再执行，并保管请求、尝试、回执和核验。超时或断连不能直接等同失败；`失败`、`结果未知`、`拒绝`、`不可用`和`完成`必须保持不同语义。
- 慢模型、网络和工具 I/O 不得处于权威数据库写事务内。并发主体变化不得静默使用最后写入者覆盖；重试不得重复制造经历或外部效果。
- 正式能力缺失或读取失败时明确失败、不可用或过期，不使用 mock、fixture、占位文案或角色化表达伪造成真实闭环。

## 5. 修正阶段边界

- 修正仍须走正式权威路径：耐久接纳 → 机会 → 认知 → 领域校验 → 主体提交 → 回应、行动或不行动 → 外部效果 → 记录 → 重启恢复。不得以脚本、模拟数据或事后补写制造通过。
- 首版不预建多租户、多 ARMI、跨设备多活、微服务、通用插件市场、运行时热加载或通用多 Agent 编排。
- PostgreSQL 是唯一权威关系数据库。开发与测试服务端固定使用 Docker 中的 PostgreSQL 18.4 + pgvector 0.8.6；容器不是第二事实源，Runtime 仍只通过 DSN 连接。当前开发数据库全部可丢弃、可重建，唯一建表真源是 `apps/armi-runtime/src/armi_runtime/composition/runtime_resources/schema/current/`。修改表结构时直接修改这里的当前 DDL，重置数据库后执行 `armi db install`；不得新增 numbered migration、`schema_migrations`、目标版本、迁移摘要或升级兼容代码。只有首次启用需要保留真实用户数据的环境时，才另立任务设计迁移基线。
- 新能力通过端口和适配器接入。可替换策略返回候选，不取得领域 current state 的所有权；只有跨模块语义改变时才修改公共契约及其消费者。
- 当前仍是高迭代修正环境。默认禁止为内部策略、实现选择、阶段状态、摘要传播或“看起来完整”新增 JSON manifest、inventory、evidence contract 或跨 distribution 镜像；这些事实优先放在代码常量、类型、当前 DDL 和测试中。
- 只有产品当前直接读取的机器合同才保留 JSON：外部 wire/schema、实际 provider/model binding、静态前端资源索引和包管理锁文件。数据库 schema/权限、阶段完成状态、候选身份和验收结果不维护 JSON 副本；没有产品消费者、只被测试反向验证存在的治理 JSON 直接删除。
- 提交到仓库、供人检查的 JSON 必须使用 UTF-8、2 空格缩进和结尾换行；RFC 8785 只用于摘要输入或 wire 编码，不得作为把仓库文件压成单行的理由。运行时配置 schema 默认由代码按需导出，不提交重复 schema/摘要镜像。
- Context policy、candidate policy 及类似单进程内部机制以代码 contract version 区分，不保存重复 policy JSON。普通修正不生成结构化 evidence JSON；正式发布机制按用户以后明确指导的实际需要设计，不预留 bundle identity 或验收治理链。
- 当前开发期不做数据库升级兼容：公共耐久字段变化也直接修改当前 DDL 和消费者，旧开发库重建。外部 wire 或跨 distribution 资源确需版本时才保留最小版本字段；生成镜像只能由生成器维护，禁止手工逐份编辑。
- `runtime-composition` 只记录接缝与 Active binding，不聚合配置、前端、schema 或策略文件摘要。各资源只在真实消费边界验证，前端构建不得顺带校验 Runtime composition。
- 普通 Creator 对话默认只进行一次主模型调用，模型只返回本轮真正作出的决定和必要正文；模型 identity、usage、固定主体/场景/版本/digest/basis 等由适配器或 Runtime 绑定。不得重新让模型回显整套数据库合同，也不得为普通对话引入多模型流水线；只有独立任务确有不同能力或证据需求时才增加调用。
- ARMI→Codex 使用官方 SDK 和用户订阅认证。每项委托可显式选择 `gpt-5.6-sol`、`gpt-5.6-terra` 或 `gpt-5.6-luna`、思考级别与内置 Web Search；不得退回固定 Sol、固定低思考或一律禁网。一次性 workspace 默认可操作，安全边界是不得逃出 workspace、不得触碰显式 forbidden paths、宿主秘密/管理面/用户配置或未经授权的外部写入；不要用脆弱的逐文件 allowlist 或“只能写 result.md”冒充通用安全边界。纯内容任务可由结构化 deliverable 落为 `result.md`，代码/文件任务按任务 manifest 和独立 validator 核验。
- Codex 的内置 Web Search 是按任务显式启用的只读模型工具，不等于生成 shell 获得任意网络；用户 MCP、插件、hooks、任意 endpoint 和宿主凭据仍不得继承。Codex 委托能力与 ARMI 自身的网页证据能力是两条独立链路，必须分别验收。
- ARMI 是高迭代实验系统。每次修正先区分固定不变量、版本化公共契约、可替换机制、可重建投影和隔离实验制品；只为当前真实变化建立窄接缝，不预建通用插件、万能数据模型或永久兼容层。
- 高变化机制接收冻结且获准的输入，只返回候选、决定、证据或回执；具体实现只在 composition root 选择。实验 variant 只能在隔离环境、离线回放或只读 shadow 中比较，未激活前不得写 Active 主体、工作或效果。
- 外部服务、凭据或真实数据缺失时保持明确失败，不私自接入生产环境、真实账号或付费能力。
- 对用户明确启动的修正，可使用该任务已经获准的 provider、模型、API 地址、credential locator 和隔离环境，自主执行完成验收所必需的真实付费测试及基于明确失败根因的有限重试，无需逐次询问。仍须保持最小必要调用、记录 usage/费用并脱敏证据；更换 provider、账号、credential 或连接生产资源仍须另行授权。

## 6. 变更与验证

- 文档变更至少检查：术语一致性、编号唯一性、相对链接、状态、上下游追踪、阶段范围以及与相关不变量是否冲突。
- 优先用 `rg` 查找被修改的术语、编号、表名和旧表述在全库中的引用；不能只检查当前文件。
- 架构或契约变更须同步验证责任所有者、写入路径、事务边界、失败语义、权限边界、并发与恢复路径。
- 变更先按内部重构、机制替换、公共契约、权威 schema 或不变量/owner 分类；命中后者时不得伪装成普通 adapter 重构。机制替换完成后，删除当前实现中的旧入口、旧接线、旧 selector、旧配置和无调用者兼容路径，并证明每个接缝只有一个 Active 默认实现。
- 数据库变更须同步修改当前 DDL、查询/写入代码和直接相关测试；只验证当前空库能建立正确结构，不编写旧版本升级、回填或迁移账本测试。
- 进入代码修正后先运行覆盖当前问题与主路径的最小静态检查和定向测试。失败、并发、重复执行和崩溃恢复在代码结构明显依赖它们时补最小用例；完整安全矩阵、部署和发布不因普通修正自动启动。
- 开发循环默认只运行与本次改动直接相关的格式、类型、单元、契约和必要数据库用例；只有改动命中仍被真实消费者读取的生成物时才复算该生成物。先用最小测试定位并修复问题，不得把完整质量入口、完整 PostgreSQL 集成、结构化证据生成或双 clean-root 当成每次小改后的默认动作。
- 完整质量门禁、真实 PostgreSQL 集成、安装、真实浏览器、付费 live gate、外部程序验收和 soak 只在当前修正的具体风险需要或用户明确要求时运行，不得无依据扩大验证范围。
- 全量门禁失败后，先定向复验失败 gate 及其直接依赖。只有修复触及共享契约、权威 schema、依赖锁、生成器、组合根或跨模块公共边界时，才在收尾补跑一次全量；纯格式、换行、镜像同步或单点测试期望修正不得触发反复全量。
- 每次扩大验证范围前，应能说明该范围覆盖的具体风险；已通过且未受后续改动影响的昂贵门禁不重复执行。最终报告分别列出定向验证、最终全量验证和未运行项。
- 收尾时报告实际修改、验证证据、仍依赖的草案或未验证环境，以及本次提交信息；不得把文档推演写成运行验证通过。
