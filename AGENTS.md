# ARMI 项目协作规则

本文件只补充 ARMI 特有的长期事实和工作边界。通用的安全、Git、Windows、实现与验证规则继续遵循上级 `AGENTS.md`。

## 1. 开始工作前

- 先读根目录 `README.md`、`docs/README.md`、与任务直接相关的叙述性设计，以及当前代码、配置和测试。
- `docs/` 是私有设计资料并被 Git 忽略，但仍须在设计变化时同步维护。不要因为它不在 Git 状态中就跳过检查。
- 精确实现事实依次以当前代码、冻结的 `schema/baseline/`、Alembic revisions、配置、锁文件、测试和目标环境证据为准。叙述性文档说明意图、边界和方案，不替代字段级合同。
- `docs/00-外部研究参考/` 只提供证据和候选思路。外部项目自述、源码观察、复现实验和 ARMI 推断必须分开；研究结论只有被 ARMI 设计正文吸收后才生效。
- 项目尚未授予开源许可证。不得擅自声明开源、复制不兼容代码，或删除研究资料中的来源与许可证记录。
- 后续工作由用户逐项指导。不要从已删除的阶段编号、历史路线图或验收清单自行推导下一项开发。

## 2. 产品不变量

- ARMI 是承载一个自主电子人长期存在的运行时，不是通用助手、多角色平台、多租户产品或多 Agent 编排框架。
- 正常运行中只有一个 ARMI、一条权威生命线和一个当前被承认的活动运行世界。模型、进程、设备、入口、场合、活动与外部 Agent 都不是另一个主体。
- 主体、生活、关系、权限与外部效果各有唯一事实所有者和正式写入路径。客户端、缓存、索引、摘要、向量与其他派生读取不能反向覆盖权威事实。
- 主观记忆与客观记录严格分离。日志不能在日常认知中补全已遗忘内容，记忆变化也不能改写正常运行记录。
- 系统权限回答“能不能”，ARMI 的主观意愿回答“想不想”。创造者的系统管理权不等于社会关系中的绝对命令权。
- 模型、网页、平台、工具和执行器都在信任边界外，只能返回候选、证据或回执，不能直接提交主体状态、扩大权限或定义现实结果。
- 对外表达与现实动作必须先登记再执行，并保存请求、尝试、回执和核验。`失败`、`结果未知`、`拒绝`、`不可用`与`完成`不得合并。
- 正式能力缺失、配置错误或读取失败时明确失败，不使用 mock、fixture、占位文案或角色化表达伪造成真实闭环。

## 3. 架构与实现边界

- 首版采用单一权威 Runtime 的模块化单体，依赖方向为 `Interface → Application → Domain`；适配器通过稳定内核端口接入。领域不得依赖具体模型 SDK、平台 SDK、数据库对象或客户端格式。
- 慢模型、网络、文件和工具 I/O 不得处于权威数据库写事务内。先用短事务登记稳定身份，事务外调用，再用短事务保存结果并重新校验当前主体版本。
- 并发主体变化不得静默采用最后写入者覆盖。重试、恢复和重复输入不得重复制造经历或外部效果。
- 高变化机制接收冻结且获准的输入，只返回候选、决定、证据或回执；具体实现只在 composition root 选择。实验 variant 只能在隔离环境、离线回放或只读 shadow 中比较，未激活前不得写 Active 主体、工作或效果。
- 新能力通过端口和适配器接入。只在跨模块语义真实改变时修改公共契约，并同步全部消费者；机制替换完成后删除旧入口、旧接线、旧 selector、旧配置与无调用者兼容路径。
- 不为未来可能性预建微服务、多 ARMI、跨设备多活、通用插件市场、运行时热加载、万能数据模型或永久兼容层。

## 4. 数据库与机器合同

- PostgreSQL 是唯一权威关系数据库。开发与测试服务端固定使用 Docker 中的 PostgreSQL 18.4 + pgvector 0.8.6；Runtime 只通过 DSN 连接，容器不是第二事实源。
- 数据库结构由 Alembic 的单线 revision 管理，只允许一个 head，不使用 autogenerate 或 ORM metadata，也不提供 downgrade。`0000` 在同一事务中执行 `schema/baseline/` 的有序领域模块并直接形成当前基线；这些模块只是一个 revision 的职责拆分，冻结后不得回头改写。后续持久结构变化新增普通 Python revision，并同步写入/查询代码与相关测试。
- `armi db install` 只允许空数据库并直接升级到 Alembic head；后续升级使用 `armi db migrate --apply`。Runtime 和普通启动脚本只检查 PostgreSQL/扩展身份、角色安全策略与当前 revision，不自动安装或迁移，也不以完整 catalog 指纹作为 readiness 门禁。
- 正式环境的唯一权威数据库不得因源码或 DDL 变化而重建。测试数据库仍可丢弃重建，但必须与正式环境隔离；迁移前停止 Runtime，并按数据风险完成可核验备份与恢复准备。
- 只有具备独立 owner、生命周期、一对多关系、权限/保留策略或显著独立查询模式时才新增表；渠道、party kind、枚举值或 adapter 差异本身不构成建表理由。
- 只有产品当前直接读取的机器合同才保留 JSON，例如外部 wire/schema、实际 provider/model binding、静态前端资源索引和包管理锁文件。内部策略、阶段状态、候选身份与验收结果不维护 JSON 镜像。
- 仓库中的 JSON 使用 UTF-8、2 空格缩进和结尾换行。RFC 8785 只用于摘要输入或 wire 编码；配置 schema 默认由代码按需导出，不提交重复副本。
- 人工维护的业务与部署配置集中在根目录 `configs/` 并使用 YAML；OpenAPI、JSON Schema、包管理锁、生成资源索引和 wire 等机器合同继续使用其规定格式。环境根配置同样使用 YAML，秘密只以 locator 出现。
- Runtime 接线只由 Python 组合根定义，不维护重复的 JSON 清单或把接线摘要绑定到主体连续性。资源在各自真实消费边界验证。

## 5. 模型、Codex 与管理面

- 普通 Creator 对话默认只进行一次主模型调用。模型只返回本轮真正作出的决定与必要正文；模型 identity、usage、主体/场合身份、版本、digest 和依据由适配器或 Runtime 绑定。
- ARMI→Codex 使用官方 SDK 和用户订阅认证。每项委托可显式选择当前获准的 `gpt-5.6-sol`、`gpt-5.6-terra` 或 `gpt-5.6-luna`、思考级别和内置 Web Search；不要退回固定模型、固定低思考或一律禁网。
- Codex 一次性 workspace 默认可操作，但不得逃出 workspace、触碰 forbidden paths、宿主秘密、管理面、用户配置或未经授权的外部写入。纯内容任务可形成结构化 `result.md`，代码与文件任务按 task manifest 和独立 validator 核验。
- Codex 内置 Web Search 是按委托显式启用的只读模型工具，不等于 shell 获得任意网络。它与 ARMI 自身的网页证据能力是两条独立链路。
- Codex→ARMI 管理 MCP 与 ARMI→Codex 执行适配器必须隔离，不能互相发现、调用、继承凭据或串联权限。
- 用户要求通过 Codex 自动向运行中的 ARMI 发话或执行本机日常控制时，使用当前可用的 Admin MCP 或 `armi` CLI 正式入口，二者均可。Creator 输入必须经过正式 intake 并携带稳定幂等键，不能直接写数据库或伪造浏览器会话；只有用户明确要求操作或验证界面时才使用浏览器。
- 外部服务、凭据或真实数据缺失时保持明确失败。已明确启动的任务可使用该任务已获准的 provider、模型、API 地址、credential locator 与隔离环境完成必要真实验证；更换账号、凭据、provider 或连接生产资源仍需另行授权。

## 6. 文档与变更

- `docs/` 只保留能够帮助后续开发理解“ARMI 是谁、为何这样设计、系统如何承载她”的叙述性文档，以及来源清楚的外部研究。
- 不恢复需求编号矩阵、阶段施工清单、排期、单次验收流水、门禁输出、临时评审清单或字段级文档镜像。精确字段、路由、表、状态值和依赖版本放在代码、DDL、schema、配置、锁文件与测试中。
- 文档使用简体中文、UTF-8 与 LF，项目内链接使用相对路径。只保留当前有效结论；修改后清理失效术语、入口、重复定义与链接。
- 设计变化先判断它属于产品不变量、跨模块契约、可替换机制、权威 schema、可重建投影还是隔离实验制品。触及不变量、owner、权限或效果语义时，先修改最接近事实源头的叙述，再同步代码和下游文档。
- 不为显得完整而填写占位方案、模拟结果或未经确认的技术选型。未知项写清边界和所需证据。

## 7. 验证与收尾

- 代码修正先运行覆盖当前问题和主路径的最小静态检查与定向测试。只有风险实际命中共享契约、权威 schema、依赖锁、生成器、组合根或跨模块边界时才扩大验证。
- 完整质量门禁、真实 PostgreSQL、安装、真实浏览器、付费 live gate、外部程序、soak、部署与发布验证只在本次风险需要或用户明确要求时运行。
- 数据库变化至少证明空库能够安装模块化 `0000` 到唯一 head，并用真实 PostgreSQL 验证新增 revision 的前态、执行、后态、重复执行和失败时版本不前移；涉及回填时还要验证失败回滚与数据保持。
- 文档变化至少检查相对链接、旧目录/术语引用、与当前代码事实的冲突和 UTF-8/LF。未运行的环境验证必须明确说明。
- Git 仓库中的修改按可独立验证的小模块提交；只暂存本任务文件。`docs/` 不进入提交，最终报告同时说明本地文档变化与实际提交内容。
